---
feature: 2.5
keyword: pages
title: Projecao paginada 4K — remove o teto de RAM, mantem o cat
state: planned
requirements:
  - "Projecao canonica vira paginas 4K padded, escritas MANUALMENTE. E o que torna tudo possivel: o stringify do YAML so sabe emitir o documento inteiro — e ele a origem do O(n). Gerenciando a escrita, o texto continua texto."
  - "Legibilidade preservada: uma chave por linha, ordenadas, padding com espacos ate fechar a pagina; paginas alinhadas em 4096 e um writeSync posicionado por pagina. cat mostra o estado, sed -n mostra uma pagina."
  - "Acesso randomico: carrega so a pagina que contem a chave. Hoje projection e uma variavel de closure viva (io-engine.js:114) reconstruida inteira a cada open() — o store NAO PODE exceder a memoria do processo. Esta feature remove esse teto."
  - ".yaml legivel deixa de ser reescrito periodicamente; passa a ser derivado SOB DEMANDA (quem quer olhar, pede). Hoje ele e escrito a cada 100 flushes e nunca lido de volta (stringify 3x, parse de YAML 0x)."
  - "ARMADILHA CENTRAL — a semantica nao e comutativa. merge tem TOMBSTONES (null deleta, io-engine.js:401-410) e append MUTA acc in-place (:411). Ordenar por chave numa pagina nao pode perder a ordem de aplicacao: {a:1} depois {a:null} deleta, o inverso mantem. Um store append e uma LISTA ORDENADA, nao um mapa — provavelmente precisa de layout de pagina diferente do merge/assign. Risco numero um da feature."
  - "Bug pre-existente a corrigir junto: io-engine.js:245 usa { ...projection } onde o caminho normal usa _projCopy() (:229) — num store append (projecao = array) isso converte silenciosamente a projecao em objeto. So atingivel no ramo de re-verificacao sob concorrencia, que e justamente onde ninguem olha."
  - "critério: projecao identica antes/depois em TODOS os reducers (append, merge, assign); escrita O(paginas sujas); store maior que a RAM abre e responde get."
verify_tests:
  - "bun ../utest/utest.js . --force"
verify_manual: "round-trip por reducer; tombstone sobrevive a paginacao; ordem preservada em append; cat da pagina e texto legivel; store maior que a RAM configurada abre e responde get"
verify_confirmed: false
confirmed_at: null
updated: 2026-09-07
files:
  - io-engine.js
---
# [2.5] Projecao paginada 4K

## Objetivo

O maior dos cinco, e o que remove o **teto de RAM**: hoje a projecao precisa caber inteira
na memoria do processo, e `open()` a reconstroi do zero lendo o log todo.

Paginas 4K padded, escritas manualmente, dao acesso randomico por chave sem perder o `cat` —
o arquivo continua sendo texto. A condicao para isso e nao delegar ao `stringify` do YAML,
que so sabe emitir o documento inteiro.

O risco esta na semantica: `merge` tem tombstones e `append` e uma lista ordenada. Ordenar
por chave dentro da pagina nao pode destruir a ordem de aplicacao.

## Proxima acao

Aguardando 2.4.
