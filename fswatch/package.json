{
  "name": "fswatch",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Filesystem observation for Bun/Linux. A subpackage of iodb: it imports the iodb engine by relative path and is not installable on its own.",
  "main": "fswatch.js",
  "scripts": { "test": "utest fswatch.t.js --force" }
}
